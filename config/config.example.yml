default: &default
  settings:
    # Filter out Image records with sensitive human remains
    hide_sensitive_image_records: false
    # Hide sensitive human remains images with a placeholder image
    hide_sensitive_images: false
test:
  <<: *default
development:
  <<: *default
production:
  <<: *default
  settings:
    hide_sensitive_image_records: false
    hide_sensitive_images: true
