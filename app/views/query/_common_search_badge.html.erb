<span
  <% if session_value.present? %>
    class="pull-right badge" aria-hidden="false"
  <% else %>
    class="pull-right badge hidden" aria-hidden="true"
  <% end %>
  title="Options selected">
  <%= session_value.present? ? session_value.size : 0 %>
</span>
